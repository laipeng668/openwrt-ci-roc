### =========================
### General.config（通用配置）
### 目标：跨机型通用、稳定；不包含 Target/机型选择；不包含 NOWIFI 强制禁用项
### 适配：你现在的环境（main-nss 分支 + LuCI + 需要 mihomo）
### =========================

#### 编译通用选项 ####
CONFIG_DEVEL=y
CONFIG_TARGET_OPTIONS=y
# 建议用 -O2 更稳，-O3 偶尔会引入奇怪问题（尤其是第三方包）
CONFIG_TARGET_OPTIMIZATION="-O2 -pipe -march=armv8-a+crypto+crc -mcpu=cortex-a53+crypto+crc -mtune=cortex-a53"
CONFIG_CCACHE=y
CONFIG_TARGET_PER_DEVICE_ROOTFS=y

#### 默认设置/语言 ####
CONFIG_PACKAGE_default-settings=y
CONFIG_PACKAGE_default-settings-chn=y
CONFIG_LUCI_LANG_zh_Hant=y

# 不构建 initramfs
# CONFIG_TARGET_ROOTFS_INITRAMFS is not set

#### ZRAM ####
CONFIG_PACKAGE_zram-swap=y
CONFIG_PACKAGE_kmod-lib-lz4=y
CONFIG_KERNEL_ZRAM_BACKEND_LZ4=y
# CONFIG_KERNEL_ZRAM_BACKEND_LZO is not set
CONFIG_KERNEL_ZRAM_DEF_COMP_LZ4=y

#### 基础工具（通用）####
CONFIG_PACKAGE_tmux=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_btop=y
CONFIG_PACKAGE_iperf3=y

CONFIG_PACKAGE_fdisk=y
CONFIG_PACKAGE_cfdisk=y
CONFIG_PACKAGE_sgdisk=y
CONFIG_PACKAGE_tune2fs=y
CONFIG_PACKAGE_fstrim=y

CONFIG_PACKAGE_nano-full=y
CONFIG_PACKAGE_autocore=y
CONFIG_PACKAGE_coremark=y

CONFIG_PACKAGE_hdparm=y
CONFIG_PACKAGE_smartmontools-drivedb=y
CONFIG_PACKAGE_zoneinfo-asia=y

# SSH
CONFIG_PACKAGE_openssh-sftp-server=y

#### LuCI 常用（通用）####
# 一些 LuCI APP 会自动拉起 luci / rpcd 等基础包，无需你手动写 luci=y
CONFIG_PACKAGE_luci-app-cpufreq=y
CONFIG_PACKAGE_luci-app-upnp=y
CONFIG_PACKAGE_luci-app-autoreboot=y
CONFIG_PACKAGE_luci-app-uhttpd=y
CONFIG_PACKAGE_luci-app-diskman=y
CONFIG_PACKAGE_luci-app-hd-idle=y
CONFIG_PACKAGE_luci-app-acme=y
CONFIG_PACKAGE_luci-app-arpbind=y
CONFIG_PACKAGE_luci-app-watchcat=y

# 终端（如果你需要）
CONFIG_PACKAGE_luci-app-ttyd=y
CONFIG_PACKAGE_ttyd=y

# 包管理界面（不同源码树名字可能不同；你仓库里如果是 luci-app-package-manager 就保留这一行）
CONFIG_PACKAGE_luci-app-package-manager=y

# WireGuard
CONFIG_PACKAGE_luci-proto-wireguard=y

#### HTTPS/证书（通用，可选）####
# 如果你确定要 uhttpd https 或某些插件需要 openssl 工具，可打开：
CONFIG_PACKAGE_openssl-util=y
CONFIG_PACKAGE_ca-certificates=y

#### MiHoMo（通用：你说必须要）####
# 注意：mihomo / mihomo-client 以你 feeds 实际提供为准（你之前用的是 mihomo-client）
CONFIG_PACKAGE_mihomo-client=y
CONFIG_PACKAGE_luci-app-mihomo=y
CONFIG_PACKAGE_luci-i18n-mihomo-zh-cn=y

#### 可选项（先不默认启用，避免引入额外依赖/冲突）####
# DDNS（你想要再打开）
# CONFIG_PACKAGE_luci-app-ddns is not set
# CONFIG_PACKAGE_ddns-scripts-cloudflare is not set

# SQM（你想要再打开；NOWIFI/有线也能用，但依赖链长，建议后加）
# CONFIG_PACKAGE_luci-app-sqm is not set
# CONFIG_PACKAGE_sqm-scripts-nss is not set

# WOL（纯有线也能用，但不是必须）
# CONFIG_PACKAGE_luci-app-wol is not set

# aria2/ariang（如果你确实要下载器再打开）
# CONFIG_PACKAGE_luci-app-aria2 is not set
# CONFIG_PACKAGE_ariang is not set
